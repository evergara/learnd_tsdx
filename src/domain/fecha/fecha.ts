export class Date {
  weekDay(): string {
    return '';
  }

  egual(date: Date): boolean {
    return false;
  }

  difference(date: Date): number {
    return 0;
  }
  isHoliday(): boolean {
    return true;
  }
  
  show(): void {
    console.log('Fecha');
  }
}
